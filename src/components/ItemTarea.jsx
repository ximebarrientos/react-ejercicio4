import { Button, ListGroupItem } from "react-bootstrap";

const ItemTarea = () => {
    return (
        <ListGroupItem className="d-flex justify-content-between">
            dg<Button className="btn btn-danger">X</Button>
        </ListGroupItem>
    );
};

export default ItemTarea;